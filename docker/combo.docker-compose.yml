services:

  homelogger:
    build:
      context: ..
      dockerfile: Dockerfile
    volumes:
      - homelogger-data:/root/data
    environment:
      - DB_DIALECT=sqlite
    ports:
      - "3005:3005"
    restart: unless-stopped

volumes:
  homelogger-data:
 
