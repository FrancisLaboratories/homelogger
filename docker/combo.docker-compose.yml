services:

  homelogger:
    build:
      context: ..
      dockerfile: Dockerfile
    volumes:
      - homelogger-data:/root/data
    environment:
      - DB_DIALECT=sqlite
    ports:
      - "3005:80"
    restart: unless-stopped

volumes:
  homelogger-data:
 
